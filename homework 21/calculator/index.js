const add = require('./add');
const div = require('./div');
const mult = require('./mult');
const sub = require('./sub');

module.exports = {
    add: add,
    div: div,
    mult: mult,
    sub: sub
}